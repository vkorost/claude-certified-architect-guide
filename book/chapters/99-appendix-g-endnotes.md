# Appendix G: Endnotes

*Superscript numbers in square brackets in the body text refer to the per-chapter endnotes below.*

*Sources are labelled by tier where it matters. Tier 1 is Anthropic product documentation at code.claude.com and platform.claude.com. Tier 2 is the official certification exam guide. Claims in this book rest on those two. Third-party certification-preparation sites are not cited, and where an earlier edition relied on one, the claim has been re-grounded or removed.*

---

## Chapter 1: The Agentic Loop

1. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Domain 1, Task Statement 1.1, which covers the design of autonomous agentic loops. The knowledge side sets out the lifecycle: a request goes out, stop_reason is read to decide whether a tool call is still pending or the turn has ended, the requested tools run, and what they return goes back in for the next pass. It also draws the line between a model picking its own next step out of context and a sequence fixed in advance by a decision tree. The skills side restates that control flow and names three termination anti-patterns: reading the stop condition out of the model's prose, leaning on an iteration cap as the primary stop, and taking the presence of assistant text as proof the work is finished.
2. Handling stop reasons, platform.claude.com/docs/en/build-with-claude/handling-stop-reasons, accessed Q2 2026
3. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Section 4, Exam Content Outline (Blueprint). Domain weight table: Domain 1, Agentic Architecture & Orchestration, 27 percent; Domain 2, 18 percent; Domain 3, 20 percent; Domain 4, 20 percent; Domain 5, 15 percent.
4. How the agent loop works, code.claude.com/docs/en/agent-sdk/agent-loop, accessed Q2 2026
5. How tool use works, platform.claude.com/docs/en/agents-and-tools/tool-use/how-tool-use-works, accessed Q2 2026
6. How the agent loop works, code.claude.com/docs/en/agent-sdk/agent-loop, re-verified live 2026-08-15. Sections: Effort level (when effort is unset, both SDKs defer to the model's default behavior; not all models support the parameter); Permission mode (six modes: "default", "acceptEdits", "plan", "dontAsk", "auto", "bypassPermissions", with the ask-rule, organization-connector, and user-interaction exceptions to "bypassPermissions").

---

## Chapter 2: Hub-and-Spoke

1. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Section 5, Exam Scenarios: any one sitting draws four of the six scenarios at random. Scenario 3, Multi-Agent Research System, frames a coordinator that delegates to subagents which search, read documents, synthesize and write up, with cited output as the goal; Agentic Architecture & Orchestration is listed first among its primary domains, followed by Tool Design & MCP Integration and Context Management & Reliability. Also Section 4, Exam Content Outline: Domain 1 at 27 percent, the largest of the five domain weights.
2. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Domain 1, Task Statement 1.2, which covers coordinator-subagent orchestration of multi-agent systems. The knowledge side establishes the hub-and-spoke shape, with one coordinator holding every channel between subagents plus the error handling and the routing; notes that a subagent starts from isolated context and picks up none of the coordinator's history by default; assigns decomposition, delegation, aggregation and the judgment of which subagents a given query warrants to the coordinator; and flags that cutting the work too fine leaves a broad research topic only partly covered. The skills side asks for coordinators that read what a query needs and select subagents to match instead of running every stage each time; for research scope divided so that subagents do not repeat each other's work; for refinement loops in which the coordinator reads the synthesis for gaps, sends targeted follow-up work back to search and analysis, and calls for synthesis again until the coverage holds; and for every message between subagents to travel through the coordinator, which is what keeps the system observable and its error handling uniform.
3. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Domain 1, Task Statement 1.6, which covers decomposition strategy for complex workflows. Knowledge side: choosing a pipeline of chained prompts fixed in advance against a decomposition that reshapes itself around what earlier steps turn up; the chained-prompt shape in which every file is reviewed on its own before one pass looks across them; and the case for an investigation plan that generates its own next subtasks. Skills side: chaining where the aspects of a review are known before it starts, adaptive decomposition where the investigation is open-ended; separating per-file work from the cross-file pass so that attention is not spread thin; and, for an open-ended task, mapping the structure first, locating what matters most, then working to a prioritized plan that is revised as dependencies surface.
4. Subagents in the SDK. code.claude.com/docs/en/agent-sdk/subagents. Sections: Overview; Benefits of using subagents; Creating subagents; AgentDefinition configuration; What subagents inherit; Parallelization; Tool restrictions; Detecting subagent invocation.
5. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Domain 1, Task Statement 1.3, which covers how a subagent is spawned and what context reaches it. Knowledge side: the Task tool as the spawning mechanism, available to a coordinator only where allowedTools grants it; context that has to be written into the prompt, since nothing of the parent carries across on its own and nothing persists from one invocation to the next; the AgentDefinition fields for description, system prompt and tool limits; and forking a session so that divergent approaches can start from one shared analysis. Skills side: putting the actual findings of earlier agents into the prompt of the next one, with the search results and the document analyses handed to synthesis named as the case; using a structured format that keeps content apart from its metadata, source URLs and document names and page numbers, so attribution survives the handoff; issuing parallel spawns inside a single coordinator response instead of spreading them over turns; and writing coordinator prompts that state the goal and the standard rather than the procedure, which is what leaves a subagent room to adapt.
6. Subagents in the SDK, code.claude.com/docs/en/agent-sdk/subagents, re-verified live 2026-08-15. Sections: Create subagents (Agent included in allowedTools auto-approves subagent invocations without a permission prompt); AgentDefinition configuration (full field table, including the `initialPrompt` field, the `'fable'` model alias, server-level MCP patterns accepted in `disallowedTools`, and `tools` omitted inheriting every tool available to subagents rather than the parent's full pool); the two behaviors changed in Claude Code v2.1.198 (subagents run in the background by default, and a subagent inherits the main session's extended thinking configuration); Cap subagent depth, concurrency, and spend (default of 20 concurrent subagents, and `maxBudgetUsd` / `max_budget_usd` compared against `total_cost_usd` so subagent requests count); Troubleshooting, "Claude not delegating to subagents" (without Agent in allowedTools, invocations fall through to the `canUseTool` callback or are denied in `dontAsk` mode) and "Filesystem-based agents not loading" (the directory watcher, the new-directory restart case, invalid frontmatter or duplicate `name`, and programmatic agents overriding filesystem agents of the same name).
7. Claude Code subagents, code.claude.com/docs/en/sub-agents, re-verified live 2026-08-15. Sections: "Let subagents spawn their own subagents" (a subagent can spawn subagents of its own, by default up to three layers below the main conversation; at the depth limit Claude Code withholds the Agent tool so the subagent does its delegated work itself and returns one summary; `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` configures the limit and a value of 1 disables nesting; omitting Agent from a subagent's `tools` list or naming it in `disallowedTools` stops that one subagent from spawning); "Fork the current conversation" (a fork is a subagent that inherits the whole conversation instead of starting fresh, started with `/subtask`, whose own tool calls stay out of the main conversation); "Available tools" (the two filters applied to subagent tool pools, and the reduced built-in set for background subagents); "Write subagent files" (YAML frontmatter with a required `name` and `description`); "Concurrent subagent limit".
8. Manage sessions, code.claude.com/docs/en/sessions, re-verified live 2026-08-15. Sections: "Resume a session" (`claude --continue` resumes the most recent session in the current directory; `claude --resume <name>` resumes the named session directly; `claude --resume` with no argument opens the session picker; a session ID resolves from any directory); "Name your sessions" (`claude -n <name>` at startup, `/rename` during a session, or the picker; the default display name and the generated session title are not resume handles); "Branch a session" (`/branch` copies the transcript and switches the running process into it; from the command line, combine `--continue` or `--resume` with `--fork-session`; "allow for this session" permission grants carry over under `/branch` because the branch runs in the same process, and a `--fork-session` run in a separate process starts without them and re-approves there). Also CLI reference, code.claude.com/docs/en/cli-reference, for the short forms `-c`, `-r` and `-n`; `--fork-session` has no short form.
9. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Domain 1, Task Statement 1.7, which covers session state, resumption and forking. Knowledge side: bringing a named session back with --resume; fork_session as the way to branch one baseline analysis into independent lines of work; the need to tell a resumed agent which of the files it already examined have moved since; and the argument that seeding a fresh session with a written summary is the more dependable option once the earlier tool results have gone stale. Skills side: carrying named sessions across working days; branching with fork_session so two candidate approaches, a testing pair or a refactoring pair, run from the same codebase analysis; judging whether the earlier context still holds well enough to resume or whether a summary-seeded restart is safer; and pointing a resumed session at the particular files that changed so that it re-reads those rather than everything.
10. Work with sessions, code.claude.com/docs/en/agent-sdk/sessions, re-verified live 2026-08-15. Sections: Continue, resume, and fork (continue finds the most recent session in the current directory, resume takes a specific session ID); Capture the session ID; Resume by ID; "Fork to explore alternatives" (a fork starts with a copy of the original's history but diverges from that point, gets its own session ID while the original's ID and history stay unchanged, leaving two independently resumable sessions; the `fork_session` field in Python and `forkSession` in TypeScript, set alongside a resume target; forking branches the conversation history and not the filesystem, so a forked agent's file edits are real and visible to any session working in the same directory).

---

## Chapter 3: Hooks and the Loop Lifecycle

1. Claude Certified Architect Exam Guide, Task Statement 1.4. Anthropic, PBC. The knowledge bullet this chapter turns on separates enforcement written as code, meaning hooks and prerequisite gates, from ordering merely requested in a prompt, and holds that wherever compliance has to be deterministic, identity checks ahead of financial operations being the guide's case, a prompt instruction on its own carries a failure rate that is not zero.
2. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Domain 1, Task Statement 1.5, which covers Agent SDK hooks used to intercept tool calls and to normalize what they return. Knowledge side: PostToolUse and its kind reshaping a tool result before the model ever reads it; interception on the outbound side to hold a call against a business rule, refunds over a ceiling given as the case; and the split between a hook, which guarantees an outcome, and a prompt instruction, which only makes it likely. Skills side: a PostToolUse hook flattening the assorted time and status encodings that different MCP tools hand back, Unix timestamps against ISO 8601 against numeric status codes, before any of it reaches the agent; an interception hook refusing a refund above the guide's $500 line and routing the case to a human instead; and preferring a hook to a prompt wherever a business rule has to hold every time.
3. Intercept and control agent behavior with hooks , Claude Code Docs, code.claude.com. Section: “Configure hooks.” The hooks field structure, event name keys, matcher arrays, and callback registration pattern are documented here.
4. Intercept and control agent behavior with hooks, code.claude.com/docs/en/agent-sdk/hooks, section "Matchers," together with the Claude Code hooks reference, code.claude.com/docs/en/hooks, section "Matcher patterns," both re-verified live 2026-08-15. The matcher's evaluation path is selected by the characters it contains: `"*"`, `""` or omitted matches all; a string of only letters, digits, `_`, `-`, spaces, `,` and `|` is an exact string or a pipe-or-comma separated list of exact strings; any other character makes it an unanchored JavaScript regular expression tested with `RegExp.prototype.test`, so `Edit.*` matches `NotebookEdit` and `^Edit$` is needed for a whole-string match. "To match every tool from a server, append `.*` to the server prefix. The `.*` is required: a matcher like `mcp__memory` or `mcp__brave-search` contains only exact-match characters, so it is compared as an exact string and matches no tool." The matcher's target varies by event: tool events match the tool name, `Notification` matches the notification type, and `UserPromptSubmit`, `PostToolBatch`, `Stop`, `TeammateIdle`, `TaskCreated`, `TaskCompleted`, `WorktreeCreate`, `WorktreeRemove` and `MessageDisplay` have no matcher support.
5. Intercept and control agent behavior with hooks, code.claude.com/docs/en/agent-sdk/hooks, re-verified live 2026-08-15. Section: "Available hooks." The table gives Python and TypeScript availability per event. Available in both SDKs: PreToolUse, PostToolUse, PostToolUseFailure, UserPromptSubmit, Stop, SubagentStart, SubagentStop, PreCompact, PermissionRequest, Notification. TypeScript only: PostToolBatch, UserPromptExpansion, MessageDisplay, StopFailure, PostCompact, PermissionDenied, SessionStart, SessionEnd, Setup, TeammateIdle, TaskCreated, TaskCompleted, Elicitation, ElicitationResult, ConfigChange, InstructionsLoaded, WorktreeCreate, WorktreeRemove, CwdChanged, FileChanged, DirectoryAdded.
6. Intercept and control agent behavior with hooks , Claude Code Docs. Section: “Outputs.” The distinction between additionalContext (appends to tool result) and updatedToolOutput (replaces tool output before Claude sees it) is documented under PostToolUse hook-specific outputs. Also: Claude Certified Architect Exam Guide, Task Statement 1.5.
7. Intercept and control agent behavior with hooks , Claude Code Docs. Section: “Available hooks.” SubagentStart fires on “Subagent initialization” with use case “Track parallel task spawning.” SubagentStop fires on “Subagent completion” with use case “Aggregate results from parallel tasks.”
8. How the agent loop works , Claude Code Docs, code.claude.com. Section: “Automatic compaction.” The PreCompact hook is described as firing “before compaction occurs” and receiving “a trigger field (manual or auto).” Also: Intercept and control agent behavior with hooks, section “Available hooks.”
9. Intercept and control agent behavior with hooks, code.claude.com/docs/en/agent-sdk/hooks, section "Forward notifications to Slack," re-verified live 2026-08-15. Notification event types include `permission_prompt`, `idle_prompt`, `auth_success`, and `elicitation_dialog`, `elicitation_complete` and `elicitation_response` for elicitation flows. "In headless SDK sessions, only the elicitation events `elicitation_complete` and `elicitation_response` fire this hook; the other types are emitted by interactive UI that SDK sessions don't run. Permission requests, for example, go to the `canUseTool` callback instead." Each notification carries a `message` field and optionally a `title`.
10. Intercept and control agent behavior with hooks , Claude Code Docs. Section: “Session hooks not available in Python.” SessionStart and SessionEnd are available as SDK callback hooks in TypeScript but are only available as shell command hooks in the Python SDK.
11. Intercept and control agent behavior with hooks , Claude Code Docs. Section: “Outputs.” permissionDecision values "allow", "deny", "ask" documented for both SDKs. "defer" noted as TypeScript-only.
12. Intercept and control agent behavior with hooks, code.claude.com/docs/en/agent-sdk/hooks, re-verified live 2026-08-15. Sections: "Outputs" (top-level fields accepted on every event, `systemMessage` shows a message to the user and `continue`, `continue_` in Python, determines whether the agent keeps running; `hookSpecificOutput` carries `permissionDecision` with the values "allow", "deny", "ask" and "defer", plus `permissionDecisionReason` and `updatedInput`; `additionalContext` appends to a tool result and `updatedToolOutput` replaces it for any tool in both SDKs, while the older `updatedMCPToolOutput` replaces MCP tool output only and is deprecated; returning `{}` allows the operation without changes); "Add context and block a tool" (the worked example pairing `permissionDecision: 'deny'`, `permissionDecisionReason` "tells the model why, so it avoids retrying", and `systemMessage` "shows the user what happened"); "systemMessage not appearing in output" ("The `systemMessage` field shows a message to the user, not the model.... To pass context to the model instead, return `additionalContext`"). Python's three-value decision type confirmed at the Python SDK reference, code.claude.com/docs/en/agent-sdk/python: `permissionDecision` is `Literal["allow", "deny", "ask"]`, with "defer" absent. Claude Code hooks reference, code.claude.com/docs/en/hooks, PreToolUse section, for "The hook can deny the call, but staying silent doesn't approve it."
13. Intercept and control agent behavior with hooks , Claude Code Docs. Section: “Modify tool input” example. The sandbox path rewrite example demonstrates updatedInput with permissionDecision: "allow". The instruction to always return a new object rather than mutating the original tool_input is from this section.
14. Intercept and control agent behavior with hooks, code.claude.com/docs/en/agent-sdk/hooks, sections "Modify tool input" and "Modified input not applied," re-verified live 2026-08-15. "Pair `updatedInput` with `permissionDecision: 'allow'` to auto-approve the modified input, or `permissionDecision: 'ask'` to show it to the user. If you omit `permissionDecision`, the modified input still applies and flows through the normal permission evaluation. With `'defer'`, `updatedInput` is ignored. Always return a new object rather than mutating the original `tool_input`." Also: "Ensure `updatedInput` is inside `hookSpecificOutput`, not at the top level."
15. Intercept and control agent behavior with hooks , Claude Code Docs. Section: “Outputs.” The continue (TypeScript) / continue_ (Python) field “determines whether the agent keeps running after this hook.”
16. Claude Certified Architect Exam Guide, Task Statement 1.5, the skills bullet on normalization. The guide's own case is a PostToolUse hook that reconciles the mismatched time and status encodings arriving from separate MCP tools, naming Unix timestamps, ISO 8601 and numeric status codes, before the agent works with any of them.
17. Intercept and control agent behavior with hooks , Claude Code Docs. Section: “Register multiple hooks.” “When an event fires, all matching hooks run in parallel… Because completion order is non-deterministic, write each hook to act independently rather than relying on another hook having run first.”
18. Intercept and control agent behavior with hooks , Claude Code Docs. Section: “Outputs” (priority rules paragraph). “When multiple hooks or permission rules apply, deny takes priority over defer, which takes priority over ask, which takes priority over allow. If any hook returns deny, the operation is blocked regardless of other hooks.”
19. Configure permissions, code.claude.com/docs/en/agent-sdk/permissions, section "How permissions are evaluated," re-verified live 2026-08-15. The six-step order is hooks, deny rules, ask rules, permission mode, allow rules, then the `canUseTool` callback. "Run hooks first. A hook can deny the call outright or pass it on. A hook that returns `allow` does not skip the deny and ask rules below; those are evaluated regardless of the hook result." Also, from the same page: "hooks run before every other step, and a hook deny applies even in `bypassPermissions` mode," and deny rules block "even in `bypassPermissions` mode." Declarative rule syntax over tool names and arguments, including `Bash(rm *)` and `Edit(path)` forms, is documented on the same page under "Allow and deny rules."
20. Intercept and control agent behavior with hooks , Claude Code Docs. Section: “Asynchronous output.” The async: true return pattern and the constraint “Async outputs cannot block, modify, or inject context into the operation since the agent has already moved on” are documented here.
21. Intercept and control agent behavior with hooks , Claude Code Docs. Section: “Matchers.” MCP tools follow the naming pattern with mcp__ prefix.
22. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Domain 1, Task Statement 1.4, which covers multi-step workflows, their enforcement and their handoffs. Knowledge side: enforcement written as code set against ordering merely requested in a prompt; the residual failure rate a prompt instruction carries where compliance must be deterministic, identity checks ahead of financial operations given as the case; and handoff protocols for escalating mid-process, carrying the customer details, the diagnosis and the recommendation. Skills side: prerequisites implemented as gates, so a downstream call cannot fire until the step it depends on has run, with a refund call held until customer lookup has produced a verified identifier; splitting a request that raises several concerns into separate items, investigating them in parallel over shared context, then assembling one answer; and compiling the handoff summary, customer identifier and root cause and refund amount and recommended action, for a human agent who never sees the transcript.
23. Intercept and control agent behavior with hooks , Claude Code Docs. Section: “Subagent permission prompts multiplying.” “Subagents do not automatically inherit parent agent permissions.”
24. Intercept and control agent behavior with hooks, code.claude.com/docs/en/agent-sdk/hooks, section "Hook timeout," re-verified live 2026-08-15. The `timeout` field on a `HookMatcher` is set in seconds; when unset the event default applies, "600 seconds for most events, 30 seconds for `UserPromptSubmit`, and 10 seconds for `MessageDisplay`," with `SessionEnd` callbacks running under a shutdown budget of 1.5 seconds by default. On timeout the callback is cancelled and its output discarded, and the consequence is event-specific: for `PreToolUse`, "Claude Code doesn't run the tool call, Claude receives a tool result stating the hook didn't respond before its timeout, and the turn continues"; for `PostToolUse` and `PostToolUseFailure`, the tool result is kept and the turn continues; `Stop` and `SubagentStop` warn and stop normally; other events log and continue.
25. Intercept and control agent behavior with hooks , Claude Code Docs. Section: “Hook not firing.” “Verify the hook event name is correct and case-sensitive (PreToolUse, not preToolUse).”

---

## Chapter 4: Designing Tools That Get Selected

1. Claude Certified Architect Exam Guide (Anthropic, Confidential NTK). Task Statement 2.1: tool descriptions as primary routing mechanism; ambiguous overlapping descriptions causing misrouting; system prompt keyword sensitivity. Sample Questions: Q2 correct answer B with explanation. Scenario 1: get_customer vs lookup_order in Customer Support agent. Task Statement 2.3: tool_choice forced selection pattern.
2. Define tools, Claude API Docs, platform.claude.com/docs/en/agents-and-tools/tool-use/define-tools, re-verified live 2026-08-15. Sections: "Choosing a model" (the latest Claude Opus model for complex tools and ambiguous queries, because it handles multiple tools better and seeks clarification when needed; Claude Haiku models for straightforward tools, noting they may infer missing parameters); "Specifying client tools" (the four-row parameter table for `name`, `description`, `input_schema` and `input_examples`; `name` must match `^[a-zA-Z0-9_-]{1,64}$`; and the pointer to the Tool reference for "the full set of optional properties available on any tool definition, including `cache_control`, `strict`, `defer_loading`, and `allowed_callers`"); "Tool use system prompt" (the API constructs a special system prompt from the tool definitions, the tool configuration and any user-specified system prompt, in that block order); "Best practices for tool definitions" (a description should cover the tool's purpose, the cases that call for it and the cases that do not, the meaning of each parameter, and any caveats or limitations including what the tool does not return, aiming for at least three to four sentences; consolidating related operations into a single tool with an `action` parameter rather than a tool per action, because fewer and more capable tools reduce selection ambiguity; namespacing tool names by service, `github_list_prs` and `slack_send_message`; designing tool responses to return only high-signal information, with semantic stable identifiers such as slugs or UUIDs rather than opaque internal references, and only the fields Claude needs for its next step); "Providing tool use examples" and its "Requirements and limitations" subsection (each example must validate against `input_schema` or the request returns 400; input examples are not supported on server tools such as web search and code execution; examples cost roughly 20 to 50 prompt tokens for simple objects and 100 to 200 for complex nested ones); "Forcing tool use" (the four `tool_choice` options and their behaviour; `any` or `tool` causes the API to prefill the assistant message, so no natural language is emitted before the `tool_use` block; the tip that combining `tool_choice: {"type": "any"}` with strict tool use guarantees both that a tool is called and that its inputs follow the schema). The thinking constraint is the note in the same section: under manual extended thinking (`thinking: {type: "enabled"}`), `any` and named forced tool use are unsupported and error, and only `auto` and `none` are compatible, while adaptive thinking, "including on models where thinking is on by default such as Claude Opus 5, supports forced tool use." This narrowing is the correction to the blanket rule the chapter previously carried.
3. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Domain 2, Task Statement 2.3, which covers how tools are spread across agents and how tool choice is configured. Knowledge side: an agent handed eighteen tools where four or five would serve faces a harder decision and selects worse; an agent holding tools outside its specialization reaches for them anyway, a synthesis agent running web searches given as the case; scoped access, under which a role gets the tools its work needs plus a small number of cross-role ones where the traffic justifies them; and the tool_choice settings, automatic selection, any tool, and one named tool. Skills side: keeping a subagent's tool set inside the role it was built for; swapping a general tool for a narrow one, a URL fetcher giving way to a document loader that validates what it is handed; granting those few cross-role tools for frequent needs while the harder cases route back through the coordinator; forcing one tool to run first, metadata extraction ahead of the enrichment tools, and letting the dependent steps fall to later turns; and setting tool_choice to any where a tool call rather than conversational prose is the required outcome.
4. Tool search tool, Claude API Docs, platform.claude.com/docs/en/agents-and-tools/tool-use/tool-search-tool, verified live 2026-08-15. Opening section, under "Tool selection accuracy": Claude's ability to pick the right tool degrades once the available set exceeds 30 to 50 tools, and because tool search loads only a focused set of relevant tools on demand, selection accuracy stays high even across thousands of tools. Same section, under "Context bloat": a typical multiserver setup can consume roughly 55k tokens in definitions before any work is done, which tool search typically reduces by over 85 percent by loading only the three to five tools needed for a request. Also "Deferred tool loading" (`defer_loading: true` keeps a definition out of the context window until Claude discovers it through search; at least one tool, normally the search tool itself, must stay non-deferred) and "When to use tool search" (ten or more tools available, definitions over 10k tokens, selection accuracy dropping as the toolset grows, aggregation of multiple MCP servers). Cited in this chapter only for the selection-accuracy threshold; the context-cost treatment belongs to Chapter 11.

---

## Chapter 5: MCP and the Built-In Toolkit

1. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Domain 2, Task Statement 2.3, knowledge side: an agent handed eighteen tools where four or five would serve faces a harder decision and selects worse. Task Statement 2.4, knowledge side: every configured MCP server has its tools enumerated when the connection is made, after which the agent holds all of them at once. Task Statement 2.4, skills side: taking an existing community server for a standard integration such as Jira, and keeping custom servers for workflows particular to the team. Task Statement 2.5, title: the six built-in tools the objective covers, namely Read, Write, Edit, Bash, Grep and Glob; Bash appears in the title while the knowledge bullets assign content search, path search, whole-file operations and targeted modification to the other five, which is what makes Bash the residual choice. Section 5, Scenario 4, "Developer Productivity with Claude": an Agent SDK build that helps engineers find their way through unfamiliar and legacy code, produces boilerplate and takes over repetitive work, drawing on the built-in tools and on MCP servers; Tool Design & MCP Integration is listed first among its primary domains, followed by Claude Code Configuration & Workflows and Agentic Architecture & Orchestration.
2. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Confidential Need to Know. Task Statement 2.4: where an MCP server is scoped, project-level.mcp.json against user-level ~/.claude.json; expansion of environment variables; MCP resources standing in as content catalogs. Task Statement 2.5: Grep for content search, Glob for path patterns, Read/Write for full file ops, Edit for targeted modification; Read+Write fallback when Edit anchor text is non-unique; incremental exploration pattern (Grep entry points, Read to trace); tracing function usage across wrapper modules.
3. Connect to external tools with MCP, Claude Code Docs, code.claude.com/docs/en/agent-sdk/mcp, re-verified live 2026-08-15. Section "Add an MCP server": configuration in code through the `mcpServers` option and from a `.mcp.json` file at the project root, picked up when the `project` setting source is enabled, which it is for default `query()` options. Section "Allow MCP tools": MCP tools require explicit permission and without it Claude sees that tools are available but cannot call them; the naming pattern `mcp__<server-name>__<tool-name>`, with a GitHub server named `github` and a `list_issues` tool becoming `mcp__github__list_issues`; `allowedTools` entries auto-approve specific tools and wildcards allow all tools from a server without listing each one. Its Note is the permission-mode material this chapter turns on: `allowedTools` is preferred over permission modes for MCP access; `permissionMode: "acceptEdits"` does not auto-approve MCP tools, covering only file edits and filesystem Bash commands; `permissionMode: "bypassPermissions"` does auto-approve MCP tools but also disables most other safety prompts, which is broader than necessary, with a pointer to the evaluation order for the prompts that remain. This is the sentence that has moved since the manuscript was written: "most other safety prompts", not all. Section "Transport types": stdio for local processes communicating over stdin and stdout; HTTP and SSE for cloud-hosted servers and remote APIs, with `"type": "sse"` shown for a server-sent-events endpoint and the statement that for the streamable HTTP transport you use `"type": "http"` instead, that in `.mcp.json` and other JSON config files `"streamable-http"` is accepted as an alias for `"http"`, and that the programmatic `mcpServers` option accepts only `"http"`; SDK MCP servers for tools defined in application code with no separate server process. Section "MCP tool search": when many MCP tools are configured their definitions can consume a significant portion of the context window, and tool search withholds definitions and loads only what Claude needs per turn; tool search is enabled by default. Section "Error handling": a `system` message with subtype `init` at the start of each query carries each server's connection status, the `status` field taking `"pending"`, `"connected"`, `"failed"`, `"needs-auth"` or `"disabled"`; pending is common for settings-file servers that do not get the full connection wait and for servers whose tool list was served from cache, and must not be treated as a failure; check for `"failed"` or `"needs-auth"` to detect servers that will not be usable, and call `mcpServerStatus()` in TypeScript or `get_mcp_status()` in Python for updated statuses later in the session. Section "Troubleshooting": the four common causes of a failed connection (missing environment variables, server not installed, invalid connection string, network issues) and the note that when Claude sees tools but does not use them, the check is whether permission was granted with `allowedTools`. Section "Authentication": the `env` field for credentials, with `${API_KEY}` in `.mcp.json` expanding environment variables at runtime.
4. Connect Claude Code to tools via MCP, Claude Code Docs, code.claude.com/docs/en/mcp, verified live 2026-08-15. Section "MCP installation scopes": the three scopes and their table of where each loads, whether it is shared with the team, and where it is stored, being Local (current project only, not shared, `~/.claude.json`), Project (current project only, shared via version control, `.mcp.json` in the project root) and User (all your projects, not shared, `~/.claude.json`). Subsection "Local scope": local is the default; a local-scoped server is stored in `~/.claude.json` under that project's path so it does not appear in your other projects; recommended for personal development servers, experimental configurations, and servers with credentials you do not want in version control; includes the worked `~/.claude.json` shape showing the server nested under `projects` and then the project path. Its Note is the vocabulary trap this chapter now carries: the term "local scope" for MCP servers differs from general local settings, MCP local-scoped servers living in `~/.claude.json` in the home directory while general local settings use `.claude/settings.local.json` in the project directory. Subsection "Project scope": `.mcp.json` at the project root, checked into version control so the whole team gets the same servers. Subsection "User scope": stored in `~/.claude.json`, available across all projects on the machine, private to the user account. Subsection "Scope hierarchy and precedence": when the same server is defined in more than one place Claude Code connects to it once, using the definition from the highest-precedence source, and the entire server entry from that source is used with fields not merged across scopes; the ordered list is local scope, project scope, user scope, plugin-provided servers, then claude.ai connectors; the three scopes match duplicates by name while plugins and connectors match by endpoint. Also "Installing MCP servers", for the statement that when configuring servers via JSON in `.mcp.json`, `~/.claude.json` or `claude mcp add-json`, the `type` field accepts `streamable-http` as an alias for `http`, because the MCP specification uses that name for the transport, so configurations copied from server documentation work unmodified. Section "Use MCP resources": resources are referenced with @ mentions in the format `@server:protocol://resource/path`, appear alongside files in the autocomplete, and are fetched and included as attachments when referenced. Section "Use MCP prompts as commands": server prompts become commands appearing as `/mcp__servername__promptname`, discovered dynamically from connected servers, taking space-separated arguments, with their results injected directly into the conversation. Section "Scale with MCP tool search": tool search is enabled by default and MCP tools are deferred rather than loaded upfront.
5. Configure permissions, Claude Code Docs, code.claude.com/docs/en/agent-sdk/permissions, verified live 2026-08-15. Section "How permissions are evaluated", the six ordered steps: hooks run first and a hook that returns `allow` does not skip the deny and ask rules below, which are evaluated regardless of the hook result; deny rules are checked next and a matching deny blocks the tool even in `bypassPermissions` mode; ask rules follow, and `AskUserQuestion` together with MCP tools whose server sets `_meta["anthropic/requiresUserInteraction"]` always fall through to the approval callback even when an allow rule matches; then the permission mode, then allow rules, then the callback. Section "Allow and deny rules": the table showing that `disallowed_tools=["Bash(rm *)"]` denies matching calls in every permission mode including `bypassPermissions`, and that tool-name globs are supported in deny rules, with `"*"` matching every tool and `"mcp__*"` matching every MCP tool across all servers. Same section, the allow-side restriction this chapter cites: allow rules accept tool-name globs only after a literal `mcp__<server>__` prefix and the server segment must be glob-free, so `mcp__puppeteer__*` and `mcp__github__get_*` are valid while an unanchored entry such as `allowed_tools=["*"]` or `allowed_tools=["mcp__*"]` is ignored with a startup warning and does not auto-approve anything. Same section, the scoping of built-in rules: rules for `Read` and `Edit` take a path pattern, and `Edit(path)` rules govern all built-in tools that write files, while `Bash` rules are scoped by command pattern such as `Bash(ls *)`. This is the basis for the chapter's claim that routing a file read through Bash presents it to the permission layer as a shell command rather than as a path-scoped read. Same section, its Warning that `allowed_tools` does not constrain `bypassPermissions`: unlisted tools are matched by no allow rule, fall through to the permission mode, and are approved there, so `disallowed_tools` is the instrument for blocking under bypass. Section "Permission modes", "Mode details", "Accept edits mode": auto- approved operations are file edits through Edit and Write plus the filesystem commands `mkdir`, `touch`, `rm`, `rmdir`, `mv`, `cp` and `sed`, applying only to paths inside the working directory or `additionalDirectories`. "Bypass permissions mode" and its Warning: hooks still execute and can block; deny rules, explicit ask rules and hooks are evaluated before the mode check; connector tools an organization set to `ask` and tools that require user interaction still fall through to the callback; the cross-session messaging safeguards still apply.
6. How the agent loop works , Claude Code Docs. code.claude.com/docs/en/agent-sdk/agent-loop. Built-in tool table; parallel tool execution: read-only tools (Read, Glob, Grep) can run concurrently; stateful tools (Edit, Write, Bash) run sequentially; MCP tool search reduces context cost for large tool sets.
7. Scale to many tools with tool search, Claude Code Docs, code.claude.com/docs/en/agent-sdk/tool-search, verified live 2026-08-15. Opening section, the two problems tool search addresses: tool definitions can consume large portions of the context window, fifty tools using ten to twenty thousand tokens, and tool selection accuracy degrades with more than thirty to fifty tools loaded at once. This is the Tier 1 threshold the chapter carries; it is a whole-surface model limit and is not the same measurement as the exam guide's four-to-five per agent design rule in [6]. Section "How tool search works": tool search is on by default; when active, definitions are withheld from the context window, the agent receives a summary and searches when a task needs a capability not already loaded, and up to five of the most relevant tools are loaded per search by default and stay available for subsequent turns; tool search costs one extra round trip the first time Claude discovers a tool, and below roughly ten tools loading everything upfront is typically faster. Section "Configure tool search": tool search applies to all registered tools whether from remote MCP servers or custom SDK MCP servers, and the SDK always loads core built-in tools such as Bash, Read and Edit upfront and does not count them toward the deferral threshold. This is the basis for the chapter's statement that the core built-in tools are exempt from deferral. Section "Limits": a maximum of ten thousand tools in the catalogue and up to five results per search. Cited in this chapter for the selection- accuracy threshold and the exemption of core built-ins; the context-cost treatment belongs to Chapter 11.

---

## Chapter 6: The CLAUDE.md Hierarchy

1. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Task Statement 3.1, on the scoping and modular organization of CLAUDE.md files. Covers: the three-level hierarchy of user, project and directory; user-level content staying with the one user and never reaching teammates; the @import syntax; the.claude/rules/ directory; the /memory command as the way to confirm which files loaded; and the diagnosis of a new colleague who receives none of the team's instructions because they sit at user level rather than project level. Task Statement 3.3, on path-specific rules, and on why a glob pattern beats a subdirectory CLAUDE.md when a convention follows a file type across the tree.
2. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Section 5, "Exam Scenarios", Scenario 2, titled "Code Generation with Claude Code": a team putting Claude Code to work on generation, refactoring, debugging and documentation, wiring it into the development workflow through custom slash commands and CLAUDE.md configurations, and knowing when plan mode earns its keep against direct execution; primary domains listed as Claude Code Configuration & Workflows and Context Management & Reliability. It contains no onboarding incident, no team lead, no worked diagnosis and no configuration example. Domain 3, Task Statement 3.2, final skills bullet: the choice between a skill, which is called up only when the particular workflow needs it, and CLAUDE.md, which carries the universal standards and is loaded every time. This is the Tier 2 half of the chapter's task-trigger boundary; the Tier 1 half is in [5].
3. How Claude remembers your project, Claude Code Docs, code.claude.com/docs/en/memory, verified live 2026-08-15. Section "Choose where to put CLAUDE.md files": the four locations and their paths, given in load order from broadest scope to most specific, being managed policy (macOS `/Library/Application Support/ClaudeCode/CLAUDE.md`, Linux and WSL `/etc/claude-code/CLAUDE.md`, Windows `C:\Program Files\ClaudeCode\CLAUDE.md`), user instructions (`~/.claude/CLAUDE.md`), project instructions (`./CLAUDE.md` or `./.claude/CLAUDE.md`) and local instructions (`./CLAUDE.local.md`), with the statement that a project instruction appears in context after a user instruction; the sharing column, team members via source control for project, just you for user and local; and that files in the hierarchy above the working directory load in full at launch while files in subdirectories load on demand. Section "Import additional files": `@path/to/import` syntax, relative and absolute paths accepted, relative resolving against the importing file rather than the working directory, imported files expanded and loaded into context at launch, and recursive imports to a maximum depth of **four** hops. This is the corrected figure; the manuscript carried five. Same section: `CLAUDE.local.md` at the project root loads alongside `CLAUDE.md` and should be added to `.gitignore`, and `/init` does that for you only when the interactive flow is enabled with `CLAUDE_CODE_NEW_INIT=1`. Section "AGENTS.md": a repository already using `AGENTS.md` gets a `CLAUDE.md` that imports it so both tools read one set of instructions. Section "How CLAUDE.md files load": the upward directory-tree walk checking each directory for `CLAUDE.md` and `CLAUDE.local.md`; all discovered files concatenated into context rather than overriding each other; ordering from filesystem root down to the working directory; `CLAUDE.local.md` appended after `CLAUDE.md` within each directory; subdirectory files discovered but included only when Claude reads files in those subdirectories; and block-level HTML comments stripped before injection while comments inside code blocks are preserved. Section "Write effective instructions": target under 200 lines per CLAUDE.md file, longer files consuming more context and reducing adherence; the specificity examples of indentation width and running the test command against their vague counterparts; and the Consistency paragraph this chapter's central correction rests on, that if two rules contradict each other Claude may pick one arbitrarily, with the remedy being periodic review of CLAUDE.md files, nested CLAUDE.md files and `.claude/rules/` to remove outdated or conflicting instructions. Section "Organize rules with `.claude/rules/`": markdown files in the directory, one topic per file, all `.md` files discovered recursively so rules can sit in subdirectories, symlink support including links to shared rule sets outside the repository, and rules without `paths` frontmatter loaded at launch with the same priority as `.claude/CLAUDE.md`. Its Note is the task-trigger boundary this chapter now carries: for task-specific instructions that do not need to be in context all the time, use skills, which load only when invoked or when Claude judges them relevant. Subsection "Path-specific rules": YAML frontmatter with a `paths` field, glob patterns, and the statement that path-scoped rules trigger when Claude reads files matching the pattern rather than on every tool use, while rules without a `paths` field are loaded unconditionally. Subsection "User-level rules": `~/.claude/rules/` applies to every project on the machine and is loaded before project rules. Subsection "Exclude specific CLAUDE.md files": `claudeMdExcludes` skips files by path or glob, patterns matched against absolute paths, configurable at any settings layer (user, project, local, managed policy), arrays merging across layers, and managed policy CLAUDE.md files never excludable. Section "View and edit with `/memory`": the command lists CLAUDE.md, CLAUDE.local.md and other memory file locations across user and project scopes, including entries for files that do not exist yet, toggles auto memory, offers to open the auto memory folder, and opens a selected file in the editor, creating it first if absent; and the sentence that has moved since the manuscript was written, that to check which files actually loaded into the current session you run `/context`. Section "Troubleshoot memory issues": CLAUDE.md content delivered as a user message after the system prompt and not as part of it, with no guarantee of strict compliance; `/context` and its **Memory files** list as the did-it-load check; and the instruction that must run at a fixed point belonging in a hook. Section "CLAUDE.md vs auto memory": both systems treated as context and not enforced configuration, with a PreToolUse hook named as the way to block an action regardless of what Claude decides. Section "When to add to CLAUDE.md": an entry that is a multi-step procedure, or that only matters for one part of the codebase, belongs in a skill or a path-scoped rule instead. Subsection "Instructions seem lost after `/compact`": project-root CLAUDE.md re-read from disk and re-injected after compaction, while nested CLAUDE.md files in subdirectories **and rules with `paths:` frontmatter** are not re-injected and reload the next time Claude reads a matching file. The rules half of that sentence is new to the chapter.

---

## Chapter 7: Slash Commands, Skills, and Plan Mode

1. Extend Claude with skills, Claude Code Docs, code.claude.com/docs/en/skills, verified live 2026-08-15. Section "Where skills live": the four locations and their paths, enterprise via managed settings, personal `~/.claude/skills/<skill-name>/SKILL.md`, project `.claude/skills/<skill-name>/SKILL.md`, and plugin `<plugin>/skills/<skill-name>/SKILL.md`; and the collision rules this chapter's central correction rests on. Across levels, enterprise overrides personal and personal overrides project, with the worked case of a `deploy` skill present in both the personal and project directories, where `/deploy` runs the personal one. A skill at any of those levels also overrides a bundled skill of the same name but not that bundled skill's aliases. Plugin skills use a `plugin-name:skill-name` namespace and cannot conflict with other levels. This is the inverse of the settings precedence order and the manuscript carried it inverted. Same section: a skill or command from any of these sources overrides a skill synced from a claude.ai account with the same name; skills also load from nested `.claude/skills/` directories below the working directory, where a nested skill sharing a name with a root skill keeps both available, the nested one under a directory-qualified name whose description records the directory it applies to, with Claude picking the variant matching the files it is working on. Note at the head of the page: **custom commands have been merged into skills**, a file at `.claude/commands/deploy.md` and a skill at `.claude/skills/deploy/SKILL.md` both create `/deploy` and work the same way, existing `.claude/commands/` files keep working, and skills add a directory for supporting files, frontmatter controlling who invokes them, and automatic loading by Claude. Section "Where skills live" states the tie-break for that case: if a skill and a command share the same name, the skill takes precedence. This is the version-sensitive material; the exam guide's two-surface model stays primary in the chapter and this behavior is recorded in a marked note. Section "Discovery from parent and nested directories": project skills load from `.claude/skills/` in the starting directory and every parent up to the repository root, `--add-dir` brings in directories outside that path, and nested skills below the starting directory are not loaded at startup but load the first time Claude reads or edits a file inside that subdirectory. Section "Share skills": project skills are distributed by committing `.claude/skills/` to version control. Section "Frontmatter reference": `argument-hint` is the hint shown during autocomplete to indicate expected arguments; `allowed-tools` names tools Claude can use **without asking permission** during the turn that invokes the skill, with the grant clearing at the next message; `disallowed-tools` is the field that removes tools from the pool while a skill is active; `context` set to `fork` runs the skill in a forked subagent context; `paths` accepts glob patterns limiting automatic activation to matching files; `agent`, `background`, `model`, `effort`, `hooks`, `disable-model-invocation` and `user-invocable` are also documented. Section "Pre-approve tools for a skill" carries the sentence the chapter's version note rests on: `allowed-tools` does not restrict which tools are available, every tool remains callable, and permission settings still govern tools that are not listed. This is a shift from the exam guide's description of the same field. Section "Run skills in a subagent": the skill content becomes the prompt that drives the subagent and the subagent does not have access to the conversation history.
2. Anthropic. “Claude Certified Architect Foundations Certification Exam Guide.” Task Statement 3.2. Commands scoped to a project under.claude/commands/; a skill living at.claude/skills/ behind a SKILL.md whose frontmatter carries the argument-hint and allowed-tools fields alongside context: fork; context: fork keeping a skill's exploration out of the main conversation; allowed-tools narrowing what the skill can reach; and the choice between a skill, called up on demand, and CLAUDE.md, loaded every time.
3. Claude Code documentation. “Slash Commands in the SDK.” code.claude.com/docs/en/agent-sdk/slash-commands. Commands dispatched as prompt strings; system/init lists available commands in the session..claude/commands/ is the legacy format;.claude/skills/<name>/SKILL.md is the recommended current format supporting both slash-command invocation and autonomous model invocation.
4. Claude Code documentation. “Agent Skills in the SDK.” code.claude.com/docs/en/agent-sdk/skills. Skills discovered from.claude/skills/ and ~/.claude/skills/; model autonomously invokes based on description; skills option on query() controls which skills are available; allowed-tools frontmatter applies to CLI only, not SDK.
5. Anthropic. “Claude Certified Architect Foundations Certification Exam Guide.” Task Statement 3.4, on plan mode against direct execution. The guide reserves plan mode for work with architectural consequences, its cases being a microservice restructuring, a library migration reaching more than 45 files, and a choice among integration approaches with different infrastructure behind them; it sends well-understood changes straight to execution, its cases being a one-file bug fix and the addition of a date validation conditional; it assigns verbose discovery to the Explore subagent; and it treats the two as combinable, planning the investigation and then executing the plan directly.
6. Choose a permission mode, Claude Code Docs, code.claude.com/docs/en/permission-modes, verified live 2026-08-15. Carries every plan-mode mechanism claim in the chapter's "Plan Mode" section. Section "Available modes": six modes, `default` (labelled Manual), `acceptEdits`, `plan`, `auto`, `dontAsk`, `bypassPermissions`, with `plan` described as reads plus classifier-approved commands where auto mode is available. Section "Analyze before you edit with plan mode": plan mode tells Claude to research and propose changes without making them; Claude reads files, runs shell commands to explore, and writes a plan, but does not edit source; except in sessions with bypass permissions available, edits stay blocked until the plan is approved. Where auto mode is available and `useAutoModeDuringPlan` is on, the classifier reviews shell commands during planning instead of prompting; otherwise commands outside the built-in read-only set prompt for approval. Plan mode is entered with `Shift+Tab`, by prefixing a prompt with `/plan`, or with `claude --permission-mode plan`, and `Shift+Tab` again leaves it without approving a plan. Approving a plan exits plan mode and switches the session to the permission mode the chosen approve option describes. Section "Skip all checks with bypassPermissions mode": in sessions with bypass permissions available Claude Code does not enforce plan mode's blocks; Claude is still instructed to plan without editing, but a file edit or shell command it attempts during planning runs without prompting. Protected-paths table, `plan` row: writes are prompted, allowed where bypass permissions are available, and otherwise routed to the classifier where auto mode is available.
7. Use subagents, Claude Code Docs, code.claude.com/docs/en/sub-agents, verified live 2026-08-15. Built-in Explore and Plan subagents. Explore is a fast read-only agent for searching and analyzing codebases, with Write and Edit denied, that Claude delegates to when it needs to search or understand a codebase without making changes, which keeps exploration results out of the main conversation context. Plan is the research agent used during plan mode to gather context before presenting a plan, also read-only, so that exploration output stays in a separate context window while the main conversation remains read-only. Both skip CLAUDE.md files and the parent session's git status to keep research fast and inexpensive, and they are the only subagents that do so, with no frontmatter field or per-agent setting to change it.
8. Anthropic. “Claude Certified Architect Foundations Certification Exam Guide.” Task Statement 3.5. Concrete input/output examples as most effective technique for inconsistent transformations; 2-3 examples; TDD iteration cycle; interview pattern for unfamiliar domains to surface design considerations; interacting issues in single message; independent issues in sequential iteration.

---

## Chapter 8: Claude Code in CI/CD

1. “Claude Certified Architect Guide.” Anthropic, PBC. Confidential Need to Know. Source for: Task Statement 3.6 (-p flag, --output-format json, --json-schema, CLAUDE.md as project context, session context isolation, prior review findings, fixture documentation); Task Statement 4.5 (Message Batches API: 50% cost savings, 24-hour window, no guaranteed latency SLA, no multi-turn tool calling, custom_id correlation, batch vs synchronous decision rule, failure resubmission by custom_id); Task Statement 4.6 (self-review limitations, independent review instances); Question 10 (correct answer: -p flag); Question 11 (correct answer: batch for overnight reports, synchronous for pre-merge checks); permissionMode: "bypassPermissions" as recommended for CI/containers.
2. “Run Claude Code programmatically.” Claude Code Docs. https://code.claude.com/docs/en/headless. Describes the -p/--print flag, --bare mode, --output-format options including json and stream-json, and the --json-schema combination. Notes that all CLI options work with -p and that --bare skips discovery of hooks, skills, MCP servers, and CLAUDE.md.
3. CLI reference, Claude Code Docs, code.claude.com/docs/en/cli-reference, verified live 2026-08-15. Carries the four CI flags the chapter previously lacked and the two it stated imprecisely. `--max-turns`: limit the number of agentic turns, print mode only, exits with an error when the limit is reached, **no limit by default**. `--max-budget-usd`: maximum dollar amount to spend on API calls before stopping, print mode only; spend from subagents counts toward the cap, and once spend reaches it, spawning another subagent fails with `Budget limit reached` and Claude Code stops background subagents still running (cap-enforcement behaviors require v2.1.217 or later). `--system-prompt`: replace the entire system prompt with custom text. `--append-system-prompt`: append custom text to the end of the default system prompt. `--append-system-prompt-file`: load additional system prompt text from a file and append it to the default prompt. `--bare`: minimal mode, skip auto-discovery of hooks, skills, plugins, MCP servers, auto memory and CLAUDE.md so scripted calls start faster; Claude has access to Bash, file read and file edit tools; sets `CLAUDE_CODE_SIMPLE`. `--json-schema`: validated JSON output matching a JSON Schema after the agent completes its workflow, print mode only; exits with an error on an invalid schema and accepts the `format` keyword as an annotation without client-side validation. `--output-format`: `text`, `json`, `stream-json`. `--permission-mode`: accepts `default`, `acceptEdits`, `plan`, `auto`, `dontAsk`, `bypassPermissions`, or `manual` as an alias for `default`; overrides `defaultMode` from settings files. `--allowedTools` / `--allowed-tools`: **tools that execute without prompting for permission**, with the explicit note that **to restrict which tools are available, use `--tools` instead**. That last line is what the chapter's previous claim about `--allowedTools` scoping a bypass session got backwards.
4. “Claude Code GitHub Actions.” Claude Code Docs. https://code.claude.com/docs/en/github-actions. Source for: claude_args parameter, --max-turns via claude_args, ANTHROPIC_API_KEY via ${{ secrets.ANTHROPIC_API_KEY }}, CLAUDE.md for behavior configuration, separate session for review.
5. Choose a permission mode, Claude Code Docs, code.claude.com/docs/en/permission-modes, verified live 2026-08-15. Grounds the rewritten "Permission Mode for CI" section, on which the exam guide takes no position at all. Mode table: `dontAsk` runs "only pre-approved tools", best for "locked-down CI and scripts"; `bypassPermissions` runs "everything", best for "isolated containers and VMs only". Section "Allow only pre-approved tools with dontAsk mode": Claude Code auto-denies every tool call that would otherwise prompt, runs only what matches `permissions.allow` rules, the read-only Bash command set, and calls approved by a PreToolUse hook; the documentation directs it at CI pipelines and restricted environments where the permitted actions are pre-defined, and the session never waits for input. It denies explicit `ask` rules rather than prompting, denies the built-in `AskUserQuestion` tool and org-`ask` connector tools even when an allow rule matches, and denies MCP tools marked `requiresUserInteraction`. Section "Skip all checks with bypassPermissions mode": the mode disables permission prompts and safety checks so tool calls execute immediately, including writes to protected paths; **explicit ask rules, org-`ask` connector tools and `requiresUserInteraction` MCP tools still force a prompt**; a Warning restricts the mode to isolated environments; on Linux and macOS Claude Code **refuses to start in this mode as root or under `sudo`** outside a recognised sandbox, with the dev container configuration named as the way to run autonomously as a non-root user; administrators can block the mode with `permissions.disableBypassPermissionsMode` in managed settings. Preamble to the mode table: permission rules "apply in every mode, including `bypassPermissions`", specifically deny rules and explicit ask rules, the org `ask` setting on connector tools, and the `requiresUserInteraction` marker, and **"Allow rules have no effect in `bypassPermissions` because everything else is already approved."** This is the Tier 1 statement that consistency with Chapter 1 (three standing exceptions) and Chapter 3 (a hook deny holds under bypass, a hook allow cannot loosen a deny) rests on.
6. Code Review, Claude Code Docs, code.claude.com/docs/en/code-review, verified live 2026-08-15. Grounds the new "Two Reviewers, Two Configuration Surfaces" subsection. Code Review is a managed service, distinct from the `claude-code-action` workflow integration, enabled once per organisation by an Owner; multiple agents analyse the diff on Anthropic infrastructure and results are posted as inline comments ranked by severity, with no workflow file to maintain. Section "Customize reviews": the service reads two files that "differ in how strongly they influence the review". `CLAUDE.md` is shared project instructions used for all tasks, read as project context, with newly introduced violations flagged at the lowest severity tier. `REVIEW.md` is a file at the repository root carrying review-only instructions, injected into every agent in the review pipeline as the highest-priority instruction block, taking precedence over the default review guidance; it is the surface for severity redefinition, nit volume caps, skip rules for paths, branches and finding categories, repo-specific checks, verification/evidence bars, re-review convergence rules and summary shape. Because it is pasted verbatim, `@` import syntax is not expanded and referenced files are not read into the prompt. Section "What the review reads and edits": the local `/code-review` command follows `CLAUDE.md` like any Claude Code session **but does not read `REVIEW.md`**.
7. Batch processing, Claude Platform Docs, platform.claude.com/docs/en/build-with-claude/batch-processing, verified live 2026-08-15. All usage charged at 50 percent of standard API prices. **Most batches complete in less than 1 hour.** Results are available when all messages have completed or after 24 hours, whichever comes first, and a batch expires if processing has not completed within 24 hours. `custom_id` correlates individual requests to their results. Expired requests are not billed. This replaces the Tier 3 half of the chapter's old `[3,5]` citation and supplies the completion-time nuance: the 24 hours is an expiry ceiling rather than an expected latency, and there is no latency guarantee anywhere in that range.

---

## Chapter 9: Prompting for Precision

1. Claude Certified Architect Foundations Certification Exam Guide, Anthropic, PBC, version 1.0 (re-checked against the current guide 2026-08-15; an earlier edition of this book cited the superseded 2026-05 draft here): Task Statement 4.1 (explicit criteria over vague instructions; false positive impact on developer trust; severity criteria with code examples; temporarily disabling high-false-positive categories); Task Statement 4.2 (2-4 targeted few-shot examples; reasoning for ambiguous-case choices; output format consistency; acceptable vs genuine-issue examples). Tier 2 source.
2. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Tier 2. Scenario framings and Task Statement 3.6. Carries: Scenario 5, a CI/CD pipeline running automated code reviews, generating test cases and providing feedback on pull requests, with prompts that give actionable feedback and minimise false positives as the stated design problem, and with Claude Code Configuration & Workflows and Prompt Engineering & Structured Output as its primary domains; Scenario 6, structured data extraction from unstructured documents with JSON schema validation, whose primary domains are Prompt Engineering & Structured Output and Context Management & Reliability; and the Task Statement 3.6 skill of emitting findings in a shape a script can parse, so that a pipeline can post them as inline comments on the pull request without a human in between.
3. Prompting best practices. Claude API Docs, platform.claude.com. `https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices`. Fetched live 2026-08-15. Tier 1. Carries: the recommended count of three to five examples for best results; the three properties an example set should have (relevant to the actual use case, diverse enough to cover edge cases and to avoid teaching an unintended pattern, and structurally separated from instructions); the `<example>` and `<examples>` tag convention; and the deprecation of last-turn assistant prefilling, which is unsupported starting with Claude 4.6 models and returns a 400 error, with system prompt instruction named as the migration path for the formatting and tone cases.
4. Define tools. Claude API Docs, platform.claude.com. `https://platform.claude.com/docs/en/agents-and-tools/tool-use/define-tools`. Fetched live 2026-08-15. Tier 1. Carries: `input_examples` as an optional array of example input objects on a tool definition; examples placed in the prompt alongside the schema; each example validated against the tool's `input_schema` with a 400 error on an invalid one; no support on server tools such as web search or code execution; and the documented token cost of roughly 20 to 50 tokens for a simple example and roughly 100 to 200 for a complex nested one. Chapter 4 owns this field; this chapter cites it only for the cross-reference and the token figures.
5. Claude Certified Architect Foundations Certification Exam Guide, Anthropic, PBC, version 1.0 (re-checked against the current guide 2026-08-15; an earlier edition of this book cited the superseded 2026-05 draft here): Task Statement 3.5, on interacting against independent issues. Where one fix bears on another, the guide has the whole set raised together in one detailed message; where the issues are independent of each other, it iterates through them one at a time. Tier 2 source.
6. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Tier 2. Task Statement 4.3, schema design. Carries the skill of designing schema fields as optional or nullable where the source document may not contain the information, on the stated grounds that a required field pressures the model into fabricating a value to satisfy it. Cited here only to support the few-shot versus required-field discrimination; the schema mechanics belong to Chapter 10.

---

## Chapter 10: Structured Output and the Validation Loop

1. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Tier 2. Section 3, exam structure, and Section 5, Exam Scenarios. Carries: that four scenarios are drawn at random from a bank of six for any given sitting; and the whole of Scenario 6, which frames a structured data extraction system that pulls information out of unstructured documents, validates the output against JSON schemas, maintains accuracy, handles edge cases without failing, and integrates with downstream systems, with Prompt Engineering & Structured Output and Context Management & Reliability as its primary domains. Cited to establish what the scenario actually contains, which is the framing and nothing beyond it.
2. Claude Certified Architect Exam Guide, Task Statements 4.3 and 4.4, both within Domain 4, Prompt Engineering & Structured Output. Anthropic, PBC. Confidential Need to Know.
3. Structured outputs, Claude API Docs. platform.claude.com. Source URL: https://platform.claude.com/docs/en/build-with-claude/structured-outputs
4. Strict tool use. Claude API Docs, platform.claude.com. `https://platform.claude.com/docs/en/agents-and-tools/tool-use/strict-tool-use`. Fetched live 2026-08-15. Tier 1. Carries: that `strict: true` constrains token sampling to schema-valid outputs by grammar-constrained sampling; the two stated guarantees, that the tool `input` strictly follows the `input_schema` and that the tool `name` is always valid; that without strict mode the model may return an incompatible type, the documented example being a string where an integer was declared, or omit a required field; that `strict` is set as a top-level property of the tool definition alongside `name`, `description` and `input_schema`; the twenty-four-hour caching of compiled tool schemas; and the HIPAA restriction that protected health information must not appear in `input_schema` property names, `enum` values, `const` values or `pattern` expressions, because compiled schemas are cached separately from message content.
5. Define tools, Claude API Docs. platform.claude.com. Source URL: https://platform.claude.com/docs/en/agents-and-tools/tool-use/define-tools
6. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Tier 2. Task Statement 2.3, tool distribution and tool choice configuration. Carries: the enumerated `tool_choice` options; the skill of using forced selection so that one particular tool runs first, with metadata extraction before enrichment tools given as the case, and with the dependent steps processed on follow-up turns rather than compelled by the same parameter; and the skill of setting `tool_choice: "any"` to guarantee a tool call rather than conversational text. Chapter 4 owns the parameter itself; this chapter cites the task statement only for the selection rule and the sequencing pattern.
7. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Tier 2. Task Statement 5.5, human review workflows and confidence calibration. Carries: field-level confidence scores calibrated against a labeled validation set as a mechanism for routing review attention; and the skill of routing extractions from ambiguous or contradictory source documents to human review. Cited here for two narrow purposes, to separate what a confidence field is documented to be good for from the fabrication problem it is often mistaken for a fix to, and to carry the human adjudication half of the two-error-source case. The task statement itself belongs to Chapter 12.

---

## Chapter 11: Context Window Discipline

1. Claude Certified Architect Foundations Certification Exam Guide. Anthropic, PBC. Task Statement 5.1, on holding critical information across a long interaction. Covers: what repeated summarization costs in precision; the lost-in-the-middle effect, under which the two ends of a long input are handled dependably and the middle is where findings go missing; the way tool output piles up and the practice of cutting it back to the fields that matter; the case facts block; ordering content with position effects in mind; scratchpad files; delegation to subagents; and crash recovery manifests, which belong to Task Statement 5.4. Task Statement 5.4 also supplies the symptoms of context degradation, answers that stop agreeing with each other and specifics giving way to generic patterns.
2. Context windows , Claude API Docs. platform.claude.com. Context window sizes: Claude Opus 4.7, Claude Opus 4.6, Claude Sonnet 4.6 have 1M-token windows; Claude Sonnet 4.5 and other models have 200K-token windows. Context awareness feature (Claude Sonnet 4.6, Sonnet 4.5, Haiku 4.5): models receive token budget information after each tool call. Context rot phenomenon: accuracy and recall degrade as token count grows, making curation of context as important as available space.
3. How the agent loop works , Claude Code Docs. code.claude.com. What consumes context in the SDK (system prompt, CLAUDE.md via settingSources, tool definitions, conversation history, tool inputs and outputs); prompt caching for static content; automatic compaction; compact_boundary event (SystemMessage in Python, SDKCompactBoundaryMessage in TypeScript); CLAUDE.md content re-injected on every request; summarization instructions in CLAUDE.md; PreCompact hook; /compact as prompt string; subagent context isolation (only final response returns to parent); MCP server context cost (all tool schemas per request); ToolSearch for on-demand tool loading.
4. Scale to many tools with tool search. Claude Code Docs, code.claude.com. `https://code.claude.com/docs/en/agent-sdk/tool-search`. Fetched live 2026-08-15. Tier 1. Carries: that tool search is on by default; that when active, tool definitions are withheld from the context window and the agent receives a summary of what is available, searching when a task needs a capability it has not loaded; that up to five of the most relevant tools load per search and stay available for subsequent turns; that fifty tools can consume ten to twenty thousand tokens; that selection accuracy degrades past thirty to fifty loaded tools; that the mechanism costs one extra round trip on first discovery, repaid by smaller context on every turn, and that below roughly ten tools upfront loading is typically faster; that previously discovered tools may be removed when the SDK compacts, after which the agent searches again; and the documented fallback conditions under which the SDK loads definitions upfront instead, namely models on the unsupported list, certain hosted deployments that reject the mechanism server-side, a non-first-party `ANTHROPIC_BASE_URL`, an explicit off setting, and the threshold mode that activates deferral only once deferrable definitions reach a configured share of the window. Cited for the correction that deferral is the default and upfront loading the fallback.
5. Context editing. Claude API Docs, platform.claude.com. `https://platform.claude.com/docs/en/build-with-claude/context-editing`. Fetched live 2026-08-15. Tier 1. Carries: the tool-result clearing strategy, which clears tool results once the conversation passes a configured threshold; that clearing proceeds oldest-first in chronological order and replaces each cleared result with placeholder text so the model knows something was removed; and that context editing is applied server-side before the prompt reaches the model, with the client keeping its own full unmodified history. Cited only for the server-side counterpart to the SDK's output-trimming advice. The beta status and the thinking-block strategy are outside this chapter's scope and are not asserted in it.
6. Explore the context window. Claude Code Docs, code.claude.com. `https://code.claude.com/docs/en/context-window`. Fetched live 2026-08-15. Tier 1. Carries the whole of the chapter's new "What Survives Compaction" section: that the system prompt and output style are unchanged because they are not part of message history; that project-root CLAUDE.md, unscoped rules and auto memory are re-injected from disk; that rules carrying `paths:` frontmatter and nested CLAUDE.md files in subdirectories are lost until a matching file is read again, because they load into message history when their trigger file is read and are summarized away with it; the stated remedy of dropping the path scoping or moving the rule to the project root; that invoked skill bodies are re-injected under a cap of 5,000 tokens per skill and 25,000 total with the oldest dropped first, and that truncation keeps the start of the file; that hooks run as code and are not a context question. Also carries: that the summarization request inherits the session's extended thinking configuration and leaves session settings unchanged; that `/compact` accepts a focus so the summary keeps what was chosen rather than what an automatic pass guesses; that the auto-compaction threshold is adjustable; and that clearing between unrelated tasks is the right move because old conversation crowds out what the next task needs and costs tokens on every message. This endnote is the source of the Chapter 6 consistency requirement and states it identically.
7. Intercept and control agent behavior with hooks , Claude Code Docs (referenced via agent loop doc). PreCompact hook fires before compaction; receives trigger field (manual or auto). Full hook mechanics assigned to Chapter 3.
8. Subagents in the SDK. Claude Code Docs, code.claude.com. `https://code.claude.com/docs/en/agent-sdk/subagents`, section "What subagents inherit". Fetched live 2026-08-15. Tier 1. Carries: that a subagent's context window starts fresh but is not empty; that it receives its own system prompt, the Agent tool's prompt, project CLAUDE.md loaded via setting sources, and its tool definitions; that it does not receive the parent's conversation history, the parent's tool results, or the parent's system prompt; and the operative consequence, that the only content passed from parent to subagent is the prompt string, so file paths, error messages and prior decisions the subagent needs must be written into it directly. Cited to correct the chapter's previous "clean conversation" phrasing, which was true about what is excluded and silent about what is included.
9. Connect Claude Code to tools via MCP. Claude Code Docs, code.claude.com. `https://code.claude.com/docs/en/mcp`, sections "MCP output limits and warnings" and "Exempt a server from deferral". Fetched live 2026-08-15. Tier 1. Carries the two context-cost facts Chapter 5 deferred to this chapter. First, output limits: a warning at 10,000 tokens on a fixed threshold; a default maximum of 25,000 tokens, adjustable through `MAX_MCP_OUTPUT_TOKENS`; the persist-to-disk fallback, in which a result over the threshold is written to a file and the tool result is replaced with an error naming that path so the agent can read the output back in portions; the per-tool `anthropic/maxResultSizeChars` annotation, which raises one tool's threshold up to a ceiling of 500,000 characters and applies to text content independently of the environment variable; and that tools returning image data remain subject to the token limit regardless. Second, deferral exemption: `alwaysLoad: true` on a server's configuration loads every tool from that server at session start regardless of the tool search setting, intended for a small number of tools needed on every turn.

---

## Chapter 12: Escalation, Provenance, and Error Propagation

1. Anthropic, PBC. “Claude Certified Architect Foundations Certification Exam Guide.” Task Statement 5.2, on escalation and on resolving ambiguity. Carries the triggers the guide treats as valid, its rejection of tone as a proxy for complexity, its handling of a lookup that returns several possible customers, and its instruction to act at once on an explicit request for a person.
2. Anthropic, PBC. "Claude Certified Architect Foundations Certification Exam Guide." Task Statement 1.5, on Agent SDK hooks used to intercept tool calls and normalize data. Tier 2. Carries: interception on the outbound side to hold a call against a business rule, the guide's case being a refund above a ceiling; and the same mechanism refusing an action that breaks policy, at the guide's $500 line, and routing it instead to a human. Cited for the business-ceiling escalation trigger, which the chapter previously attributed to Task Statement 5.2 and to a Tier 3 scenario walkthrough. The dollar figure is the guide's own example, not the book's. Task Statement 5.2 does not carry a threshold trigger.
3. Anthropic, PBC. “Claude Certified Architect Foundations Certification Exam Guide.” Task Statement 1.4, for its handoff protocols covering escalation that happens mid-process, and for the four fields the guide puts in the summary handed over, customer identifier and root cause and refund amount and recommended action, written for a human agent with no access to the transcript. Structured handoff summaries are part of the escalation and error propagation patterns in Domain 5.
4. Anthropic, PBC. “Claude Certified Architect Foundations Certification Exam Guide.” Task Statement 5.3: structured error context, access failure vs valid empty result distinction, local recovery before coordinator escalation, coverage annotations. Also Task Statement 2.2: isError flag, errorCategory, isRetryable, distinguishing transient/validation/permission/business errors.
5. Custom tools. Claude Code Docs, code.claude.com. `https://code.claude.com/docs/en/agent-sdk/custom-tools`, sections "Handle errors" and "Return structured data." Fetched live 2026-08-15 as raw markdown. Tier 1. Carries: that a handler error does not stop the agent loop, because the in-process MCP server catches uncaught exceptions and returns them as error results, so how an error is reported determines what Claude reads rather than whether the query fails; that an uncaught exception reaches Claude as the raw exception message while an error caught and returned with the failure flag reaches Claude as the message the handler composed, which can name the request that failed and what to try instead; that the flag is `isError` in TypeScript and `is_error` in Python; that its purpose is to mark a result as a failed call rather than as data that happens to look odd, and that a handler returning it lets Claude say what went wrong instead of treating a failure as a normal result; that `structuredContent` is an optional JSON object on the result, separate from the content array, holding raw values Claude reads as exact fields rather than parsing them out of text; that when it is set, text blocks in the content array are not forwarded, on the assumption that they duplicate the structured data; that the Python `@tool` decorator forwards only content and the error flag, so returning `structuredContent` from Python requires a standalone MCP server; and that a handler receives arguments already validated against its schema. Cited for the correction that `errorCategory` and `isRetryable` are not protocol fields, for the protocol-versus-execution boundary, and for the cascade-termination discussion. Note for the record: no Anthropic documentation page reserves `errorCategory` or `isRetryable`; those names are the exam guide's.
6. Connect Claude Code to tools via MCP. Claude Code Docs, code.claude.com. `https://code.claude.com/docs/en/mcp`, sections "Dynamic tool updates" and "Automatic reconnection." Fetched live 2026-08-15 as raw markdown. Tier 1. Carries: that a failed capability refresh keeps the server's previously discovered tools, prompts and resources until a later refresh succeeds, and that before v2.1.214 a transient error during the refresh replaced them with an empty list; that an HTTP or SSE server disconnecting mid-session is reconnected with exponential backoff, up to five attempts starting at a one-second delay and doubling, after which the server is marked failed; that the initial connection is retried up to three times on transient errors such as a 5xx response, a connection refused, or a timeout; that authentication and not-found errors are not retried because they require a configuration change to resolve; that post-connection capability discovery requests retry transient network and server errors up to three times with short backoff while authentication errors, 4xx responses and request timeouts are not retried; and that Claude Code tells Claude which server failed and its connection error, including in tool search results that find no matching tool, but does not report failed server connections to Claude in configurations without tool search, namely a custom `ANTHROPIC_BASE_URL`, `ENABLE_TOOL_SEARCH=false`, a model that does not support tool search, and Amazon Bedrock, Google Cloud's Agent Platform and Microsoft Foundry. The v2.1.214 regression is the chapter's live worked example of an access failure reported as a valid empty result.
7. MCP in the SDK. Claude Code Docs, code.claude.com. `https://code.claude.com/docs/en/agent-sdk/mcp`, section "Error handling." Fetched live 2026-08-15 as raw markdown. Tier 1. Carries: that the `system` message with subtype `init` reports each MCP server's connection status; that the status field takes one of `"pending"`, `"connected"`, `"failed"`, `"needs-auth"` or `"disabled"`; that `"pending"` must not be treated as a failure, since it commonly means a settings-file server that did not get the full connection wait or a server whose tool list was served from cache with the connection made on first use; and that later statuses are obtained through `mcpServerStatus()` in TypeScript or `get_mcp_status()` in Python. Cited for the third state between success and failure.
8. Anthropic, PBC. “Claude Certified Architect Foundations Certification Exam Guide.” Task Statement 5.6: claim-source mappings, conflicting statistics handling, publication/collection dates for temporal disambiguation, structured outputs preserving provenance through synthesis.
9. Anthropic, PBC. “Claude Certified Architect Foundations Certification Exam Guide.” Task Statement 5.5: stratified random sampling, accuracy by document type and field, validating accuracy before automating high-confidence extractions.
10. Anthropic, PBC. "Claude Certified Architect Foundations Certification Exam Guide." Task Statement 4.6, on review architectures built from several instances and several passes, skills section. Tier 2. Carries one bullet only: a verification pass in which each finding arrives with the model's own confidence attached, so that review effort can be routed by it. Cited solely to resolve the apparent conflict between Task Statement 5.2, which rejects self-reported confidence as an escalation trigger, and Task Statements 5.5 and 4.6, which build review routing on confidence scores. The chapter's treatment of multi-instance and multi-pass review remains assigned to Chapter 8 and this endnote is not cited for it.

---
